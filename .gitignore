# Rust build outputs
/target
/dist
# raw coverage profiles an instrumented binary can drop in its working directory
*.profraw

# test
cov.json

# ai
## setup via `just setup`
.agents/
.claude/

# other
*.lock
*.local.json
tmp/
